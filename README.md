# zun-board

서포터즈·대외활동 8개를 동시에 굴리기 위한 개인용 대시보드.

- **CLI (`zun`)** — 운영진 안내문 원문을 그대로 던지면 Claude Code CLI(구독 기반)로 파싱해 Supabase 에 넣는다. **쓰기 담당.**
- **웹 대시보드** — Cloudflare Pages 정적 배포. **읽기 + 진행상태 토글 + 산출물 기록 담당.**

파싱은 전부 `claude` CLI 서브프로세스로 처리한다. **Anthropic API 종량과금을 쓰지 않는다.**

```
├─ bin/zun.mjs                  CLI 엔트리
├─ src/db.mjs                   Supabase 클라이언트 (service_role)
├─ src/parse.mjs                claude CLI 호출 + JSON 추출
├─ src/prompt.mjs               파싱 프롬프트 템플릿
├─ src/render.mjs               터미널 표 + Asia/Seoul 날짜 유틸
├─ public/index.html            대시보드 (단일 파일, CDN만 사용)
└─ .claude/skills/zun-ingest/   Claude Code 세션 안에서 쓰는 버전
```

---

## 1. 준비

### service_role 키 받기

Supabase 대시보드 → **zungong** 프로젝트 → **Settings → API** → **`service_role`** 키 복사.

> 이 키는 RLS 를 우회합니다. 절대 깃에 올리거나 브라우저로 보내지 마세요.
> `.gitignore` 에 `.env` 가 들어 있습니다.

### .env 작성

```bash
cp .env.example .env
```

`.env` 를 열어 채웁니다:

```ini
SUPABASE_URL=https://rlisxmxytrvapkgcqkgf.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...            # 위에서 복사한 service_role
ZUN_DASHBOARD_URL=https://zun-board.pages.dev  # 배포 후 실제 주소로
```

### 설치 + 전역 등록

```bash
npm install
npm link          # 이제 어디서나 `zun` 을 쓸 수 있습니다
```

`npm link` 가 권한 문제로 막히면 `node bin/zun.mjs <명령>` 으로 그대로 쓸 수 있습니다.

### claude CLI 확인

```bash
which claude      # 경로가 나와야 합니다
```

없으면 `npm i -g @anthropic-ai/claude-code`.
다른 경로에 있으면 `.env` 에 `ZUN_CLAUDE_BIN=/path/to/claude` 를 추가하세요.

---

## 2. CLI 사용법

```bash
zun "10월 15일까지 공모전 제출"    # 그냥 말하면 된다 — 명령을 몰라도 됨
zun add                          # $EDITOR 열어서 원문 작성 → 파싱
zun add "9/18 금 10시 공공AX OT"   # 인자로 바로
cat 공지.txt | zun add             # 파이프
zun add -f 가이드북.txt             # 파일에서
zun add --activity teazr          # 활동 힌트 (선택)

zun save parsed.json             # 파싱 건너뛰고 JSON 직접 삽입 (스킬에서 호출)
zun ls                           # 다가오는 마감 30일치
zun ls 90                        # 90일치
zun done <검색어>                 # 제목 부분일치로 찾아 완료 토글
zun open                         # 배포된 대시보드 열기
zun export                       # public/calendar.ics 생성 (아이폰 구독용)
```

**옵션**

| 옵션 | 뜻 |
|---|---|
| `-f, --file <경로>` | 원문 파일에서 읽기 |
| `--activity <slug>` | 활동 힌트 |
| `-y, --yes` | 확인 없이 진행 |
| `--dry-run` | 파싱·미리보기만 하고 저장하지 않음 |
| `--all` | (`ls`) 완료된 일정도 표시 |

### `zun add` 흐름

1. 원문 확보 (인자 / 파이프 / `-f` / `$EDITOR`)
2. 프롬프트 생성 — **현재 `sp_activity` 목록과 오늘 날짜(Asia/Seoul)를 주입**해서
   "이번 주 금요일" 같은 상대 날짜와 기존 활동 매칭이 되게 한다
3. `claude -p <프롬프트> --output-format text` 실행 (타임아웃 120초)
4. 출력에서 첫 ` ```json ` 블록을 추출해 파싱 (실패하면 원문을 보여주고 재시도를 묻는다)
5. 표로 미리보기 — 활동 / 제목 / 마감(요일) / 종류 / 필수 / 충돌
6. `[Enter] 저장  [e] 수정  [q] 취소` — `e` 는 JSON 을 `$EDITOR` 로 열어 고친 뒤 다시 미리보기
7. 저장 — 새 활동은 `sp_activity` 에 insert, 일정은 `sp_item` 에 insert,
   원문과 파싱 결과는 `sp_ingest` 에 보관
8. 중복 방지 — 같은 활동 + 같은 날짜 + 제목 유사도가 높으면 건너뛸지 묻는다

기존 활동의 정보는 **덮어쓰지 않는다.** 비어 있거나 "미정" 인 칸만 새 값으로 채운다.

### 고정 주간 일정

수업 · 근로 · 배달전문점 알바는 **`public/schedule.json` 한 파일이 원본**이다.
CLI 는 파일을 직접 읽고 대시보드는 같은 파일을 `fetch` 하므로 규칙이 갈라지지 않는다.
시간이 바뀌면 이 파일만 고치면 된다.

```bash
node -e "import('./src/schedule.mjs').then(m=>console.log(m.scheduleText()))"   # 현재 시간표 확인
```

마감이나 회의가 이 시간과 겹치면 파싱 단계와 저장 단계 양쪽에서 `conflict` 로 표시되고,
대시보드 캘린더에서 겹치는 게 눈으로 보인다. 피하기 어려운 순서는 **알바 > 수업 > 근로**.

---

## 3. 대시보드

`public/index.html` 단일 파일. 빌드 스텝 없음.
`@supabase/supabase-js` 는 jsDelivr 에서 ESM 으로 불러온다.

anon 키는 클라이언트에 들어 있지만 RLS 로 보호된다 — 읽기, `sp_item` 체크오프,
`sp_output` 기록만 가능하고 일정 생성·삭제는 service_role(=CLI) 전용이다.

**구성**

1. 글로벌 내비 + 서브 내비 — 오늘 날짜, 다음 마감 카운트다운, 라이트/다크/시스템 토글
2. 이번 주 — **시간표 캘린더**. 아래 그리드는 고정 시간표(수업 과목명·교수·강의실,
   근로, 알바는 빗금)만 담고, 서포터즈 일정은 위쪽 '서포터즈' 띠에 시각과 함께 모은다.
   좁은 칸에 둘을 겹쳐 놓으면 한글 제목이 잘려서 층을 나눴다.
   1024px 미만에서는 요일별 스택으로 바뀌고 **일정 없는 날은 한 줄로 접는다**
3. **빠른 입력** — 웹에서 바로 자연어를 던진다. CLI 와 **같은 규칙 파서**
   (`public/lib/parse-rules.mjs`)를 브라우저가 그대로 import 한다.
   읽은 결과를 미리 보여주고 고른 것만 저장하며, 원문은 `sp_ingest` 에 남는다
4. 다가오는 마감 — **날짜별로 묶어서** 보여준다(날짜 머리글 + D-day + 알바 표시).
   활동별 필터(폰에서는 가로 스트립), 30일/전체 토글, 체크박스로 완료 토글
4. 활동별 카드 — 진행률 미터, 수료 조건, 혜택, 상태 배지, 누적 활동비
5. 산출물 기록 — 표 + 인라인 추가 폼, 활동별 누적 활동비 합계
6. 비어있는 정보 — `cadence` 가 "미정" 인 활동을 모아서 무엇을 물어봐야 하는지 보여줌

**디자인**

`.claude/skills/zun-design/SKILL.md` 의 ZUN 디자인 체계를 따른다 (@zun_it_ 계정과 같은 값).

- **어두운 쪽이 기본.** 바탕 4단계(`--bg` → `--panel-3`), 글자 3단계, 강조 3종.
  밝은 화면은 체계가 지정한 `#006DCC` 로 강조색을 바꿔 쓴다
- **`--accent` 위에 흰 글자를 올리지 않는다** (4.04:1). 버튼은 `--accent-btn` 바탕 +
  `--on-accent` 글자 (7.13:1)
- **`--ink-3` 는 라벨·단위 전용** (3.6:1). 사용자가 읽는 값은 `--ink-2` 이상
- 판 안에 판을 넣지 않는다 — 묶음은 섹션 제목이 한다. 그림자 없음, 모서리는 14/10px 두 가지
- 글꼴은 Pretendard 하나, 굵기 400/700/800. `word-break:keep-all`, 숫자는 `tabular-nums`
- 움직임은 **값이 바뀌는 것만** — 등장은 제자리에서(흐림), 이동·확대는 쓰지 않는다

**마이크로 인터랙션 / 접근성**

그림자를 쓰지 않는 시스템이라 움직임은 표면색·테두리·투명도로만 만든다.

- 타일 진입(최초 1회), 행 hover, 카드·칩 테두리 전환, 버튼 press `scale(.95)`
- 완료 체크 시 해당 행을 Action Blue 로 한 번 훑고 지나가는 피드백
- `prefers-reduced-motion` 에서 애니메이션·트랜지션 전부 정지
- 명암비는 라이트·다크 양쪽 모두 WCAG AA 통과 (렌더된 페이지에서 실측).
  이를 위해 흐린 글자색을 `#7a7a7a` → `#6e6e73` 으로, 채워진 버튼 배경을
  다크에서도 `#0066cc` 로 고정했다 (`#2997ff` + 흰 글씨는 3.02:1 로 미달)
- 터치 타깃 44px — 체크박스는 44px 라벨로 감싸고, 세그먼트·삭제 버튼도 44px

**반응형 — 맥북과 아이폰 기준으로 맞췄다**

| 폭 | 이번 주 | 마감 | 그 외 |
|---|---|---|---|
| 1440–1512 (맥북 13"·14") | 시간 그리드 7열 | 날짜 그룹 | **왼쪽 사이드바**, 활동 카드 3~4열 |
| 1024~1068 | 시간 그리드(축소) | 〃 | 사이드바 유지 |
| ~1023 (아이패드) | 요일 스택 | 〃 | 상단 내비로 전환 |
| ~640 (아이폰) | 요일 스택, 빈 날 접힘 | 〃 | 필터 가로 스트립, 산출물 표→카드 |

PC(≥1024px)에서는 상단 내비 대신 **왼쪽 고정 사이드바**를 쓴다 — 섹션 이동(현재 위치 표시),
섹션별 건수, 다음 마감 카운트다운, 테마 전환이 한곳에 모인다.

모든 구간에서 가로 스크롤이 없는 것을 1440·1512·834·430·393px 에서 실측했다.

### 로컬에서 보기

```bash
npm run serve     # http://localhost:5173
```

---

## 4. Cloudflare Pages 배포

정적 파일 + Pages Function 하나로 돌아간다. **빌드 스텝이 없다.**

1. [dash.cloudflare.com](https://dash.cloudflare.com) → **Workers & Pages**
   (계정에 따라 **Compute** 아래)
2. **Create application** → **Pages** → **Connect to Git**
3. `djkdb/zun_activity_management` 선택
4. 빌드 설정 — **여기가 중요하다**

| 항목 | 값 |
|---|---|
| Production branch | `claude/lucid-euler-wqemr3` |
| Framework preset | **None** |
| Build command | **반드시 비워둔다** |
| Build output directory | `public` |
| Root directory | 비워둔다 (저장소 루트) |

> `package.json` 이 있어서 Pages 가 Node 프로젝트로 감지하고 `npm run build` 를
> 넣으려 할 수 있다. 이 프로젝트엔 `build` 스크립트가 없으므로 **비워야 한다.**

5. 환경변수는 **넣지 않는다.** anon 키는 코드에 있고 그게 의도된 구조다.
   (원하면 `SUPABASE_URL` · `SUPABASE_ANON_KEY` 를 넣어 덮어쓸 수는 있다)
6. **Save and Deploy**

### 배포되면 확인할 것

```
https://<프로젝트>.pages.dev              대시보드가 뜨는가
https://<프로젝트>.pages.dev/calendar.ics  BEGIN:VCALENDAR 로 시작하는가
```

`/calendar.ics` 는 `functions/calendar.ics.js` 가 **요청마다 DB 를 읽어 만든다.**
정적 파일이 아니다 — 웹에서 일정을 넣으면 아이폰이 다음 갱신 때 바로 받아간다.

- `functions/` 는 저장소 루트에 있고 Pages 가 알아서 잡는다.
- `public/_routes.json` 이 **`/calendar.ics` 만** Function 으로 보낸다.
  나머지는 CDN 정적 서빙이라 빠르고 무료 한도도 덜 쓴다.
- `public/_headers` 가 `.mjs` 와 `schedule.json` 의 Content-Type 을 지정한다.

### 안 될 때

| 증상 | 원인 |
|---|---|
| 화면이 하얗게 비어 있다 | `.mjs` 가 잘못된 Content-Type 으로 나간 것. `_headers` 가 배포됐는지 확인 |
| `/calendar.ics` 가 404 | Build output directory 가 `public` 이 아니거나 `functions/` 가 안 올라간 것 |
| 아이폰이 구독을 거부 | `Content-Type: text/calendar` 확인. `curl -I` 로 볼 수 있다 |
| 일정이 안 보인다 | 화면의 에러 상자에 원인이 적힌다. Supabase 일시중지 여부부터 확인 |

## 5. Claude Code 안에서 쓰기

Claude Code 세션에서 안내문을 그냥 붙여넣으면 `.claude/skills/zun-ingest` 스킬이
직접 파싱해서 `node bin/zun.mjs save` 로 저장한다. CLI 를 중첩 실행하지 않는다.

---

## 6. 데이터베이스

이미 만들어져 있다. **스키마를 새로 만들거나 마이그레이션하지 마세요.**

| 테이블 | 용도 | anon 권한 |
|---|---|---|
| `sp_activity` | 활동 8개 | SELECT |
| `sp_item` | 일정 | SELECT, UPDATE (체크오프) |
| `sp_output` | 산출물·활동비 | SELECT, INSERT, UPDATE, DELETE |
| `sp_ingest` | 원문 + 파싱 결과 보관 | 없음 (service_role 전용) |

같은 Supabase 프로젝트에 다른 앱 테이블(`items` 등)이 있다.
**`sp_` 접두사 테이블만 사용한다.**

---

## 7. 알아둘 점

대시보드는 URL 만 알면 누구나 열 수 있다. 일정 체크와 산출물 기록(활동비 포함)도 마찬가지다.
개인용이라 링크를 공유하지 않으면 실질적인 위험은 낮지만, 신경 쓰이면
Supabase Auth 매직링크를 붙이면 된다.

---

## 8. 개발

```bash
npm test          # node:test — 의존성 없이 돌아간다
```

`src/core/dates.mjs` (Asia/Seoul 날짜)와 `src/providers/ai/fallback.mjs`
(규칙 기반 파서)에 테스트가 붙어 있다. 날짜는 조용히 틀리는 종류의 로직이라
여기만은 안전망을 깔아 뒀다.

### AI 실패 시 동작

`claude` 가 없거나 타임아웃이거나 JSON 을 못 뱉으면 **규칙 기반 파서로 내려간다.**
CLI 전체가 멈추지 않는다.

```
! AI 파싱에 실패해 규칙 기반으로 읽었습니다.
  `claude` 를 PATH 에서 찾을 수 없습니다.
```

규칙 기반은 `10월 15일` · `10/15` · `내일` · `다음주 금요일` · `이번 주말` ·
`이번 달 말` · `오후 3시` · `밤 11시` 정도를 읽는다. **못 읽으면 비워 두고 notes 에
적는다 — 날짜를 지어내지 않는다.** `--no-fallback` 으로 끌 수 있다.

---

## 9. 아이폰 캘린더 — 구독으로 붙인다

앱 설치도, **터미널도 필요 없다.** 대시보드 맨 아래 **아이폰 캘린더에 붙이기** 카드에서
주소를 복사해, 아이폰에서:

**설정 → 앱 → 캘린더 → 계정 → 계정 추가 → 기타 → 구독 캘린더 추가**

- `/calendar.ics` 는 **정적 파일이 아니라 Cloudflare Pages Function** 이다
  (`functions/calendar.ics.js`). 요청이 올 때마다 Supabase 를 읽어 즉석에서 만든다.
  → **웹에서 일정을 추가하면 아이폰이 다음 갱신 때 바로 받아간다.**
  파일로 두면 누가 `zun export` 를 돌려야 갱신되는데, 그러면 캘린더가 멈춘다.
- 기존 개인 일정과 **같은 화면에 겹쳐 보인다.**
- **알림이 실제로 온다** — 마감·제출은 D-7/D-3/D-1, 회의·행사는 1시간 전.
- 수업·근로·알바는 **매주 반복(RRULE)** 으로 들어간다.
- `UID` 가 안정적이라 **중복이 생기지 않는다.**
- **DB 가 죽어도 200 과 유효한 빈 캘린더를 돌려준다.** 구독이 끊기면 아이폰이
  다시 붙지 않으므로, 실패해도 구독은 살려 둔다 (원인은 `X-Zun-Error` 헤더에).

| 쿼리 | |
|---|---|
| `?schedule=0` | 수업·근로·알바를 빼고 |
| `?alarms=0` | 알림을 빼고 |
| `?name=...` | 캘린더 이름 바꾸기 |

**한계: 읽기 전용이다.** 아이폰에서 체크해도 돌아오지 않는다. 완료 체크는
웹 대시보드에서 한다.

학기 종료일을 `public/schedule.json` 에 `"term_end": "2026-12-20"` 처럼 넣으면
고정 일정 반복이 거기서 멈춘다. 없으면 16주로 잡는다.

`zun export` 는 남겨 뒀지만 **이제 선택 사항**이다 (파일로 백업하고 싶을 때만).

## 10. 웹에서 입력하기

터미널 없이 브라우저에서도 일정을 넣을 수 있다.

```
빠른 입력  →  "10월 15일까지 공모전 제출"  →  [읽기]  →  미리보기  →  [저장]
```

- **CLI 와 같은 파서를 쓴다.** `public/lib/` 의 모듈을 Node 와 브라우저가 함께
  import 한다. 빌드 스텝 없이 원본이 하나로 유지된다.
- 브라우저에서는 `claude` 서브프로세스를 못 돌리므로 **규칙 기반**으로 읽는다.
  읽을 수 있는 것:

  | 입력 | 결과 |
  |---|---|
  | `10월 15일까지 공모전 제출` | 10/15 23:59 마감 |
  | `다음주 금요일 6시에 축구` | 9/25 18:00 (맨숫자는 저녁으로) |
  | `9/14~9/20 콘텐츠 주간` | 9/14 ~ 9/20 종일 (**범위**) |
  | `10월 1일부터 10월 5일까지 자료조사` | 10/1 ~ 10/5 (**범위**) |
  | `10월 3일 오후 2시~5시 발표회` | 10/3 14:00~17:00 (**시각 범위**) |
  | `매주 화요일 팀 회의` | 8주치로 펼침 (**반복**, notes 로 알림) |
  | `네이버 미션 10월 5일까지` | 활동 `naver` 자동 연결 (**활동명 인식**) |
  | `9월 28일까지 정리하고 10월 5일까지 분석` | **2건으로 분리** |

  가이드북 붙여넣기나 주차별 표처럼 복잡한 것은 AI 가 필요하다 —
  대화창에 붙여넣으면 `.claude/skills/zun-ingest` 가 파싱해 저장한다.
- **날짜를 못 찾으면 저장하지 않고 안내한다.** 지어내지 않는다.
- 고정 일정과 겹치면 미리보기에서 경고색으로 표시된다.
- `⌘/Ctrl + Enter` 로 바로 읽기.

### 고치고 지우기

마감 목록의 각 줄 오른쪽 **수정** 을 누르면 그 자리에서 편집기가 열린다.

- 제목 · 날짜 · 시각 · 종류 · 활동 · 종일 · 필수 를 고칠 수 있다.
- **날짜나 시각을 바꾸면 고정 일정 충돌이 다시 계산된다.**
  (예: 목 19시로 옮기면 "목 18–21시 배달전문점 알바와 겹침" 이 자동으로 붙는다)
- 종일로 바꾸면 시각 칸이 잠기고 충돌도 풀린다.
- **삭제는 두 번 눌러야 한다.** 되돌릴 수 없어서 한 번은 확인을 받는다
  (4초 안에 다시 안 누르면 원래대로 돌아간다).

### 보안 — 알고 쓰세요

소유자가 개인용으로 쓴다는 전제로 **anon 키에 쓰기 권한을 열어 뒀다.**
링크를 아는 사람은 일정을 만들고 지울 수 있고, `calendar.ics` 도 공개 URL이다.
링크를 공유하지 않으면 실질적 위험은 낮지만, 신경 쓰이면 Supabase Auth
매직링크를 붙이면 된다 (그때는 구독 캘린더 URL 을 따로 처리해야 한다).

`sp_` 접두사 테이블에만 적용했고 같은 프로젝트의 다른 앱 테이블은 건드리지 않았다.
